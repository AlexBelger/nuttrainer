import {Card} from './card'
import {CATEGORIES} from './mock-categories'

export var CARDS: Card[] = [
    {"id": 2, "category": CATEGORIES[0], "first": "Q", "second":"A"},
    {"id": 3, "category": CATEGORIES[1], "first": "Q", "second":"A"}
]
